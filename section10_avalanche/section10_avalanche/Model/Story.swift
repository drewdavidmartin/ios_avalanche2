import Foundation

//creating the "story" structure (class) which will hold the title/question displayed and the two choices that the user has. Each of these variables will be strings.
struct Story{
    let title: String
    let choice1: String
    let choice1Destination: Int
    let choice2: String
    let choice2Destination: Int
}
